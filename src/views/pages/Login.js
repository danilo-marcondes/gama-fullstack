import 'bootstrap/dist/css/bootstrap.min.css'

let Login  = {
    render: async () => {
        let view = `<h1>Página de Login</h1>`
        return view
    },
    after_render: async () => {
    }
}

export default Login